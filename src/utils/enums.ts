export enum ALERT_VARIANTS {
  SUCCESS = 'success',
  WARNING = 'warning',
  DANGER = 'danger',
  INFO = 'info',
}

export enum USER_ROLES {
  ADMIN = 'ADMIN',
  PROFESSOR = 'PROFESSOR',
  STUDENT = 'STUDENT',
  // STAFF = 'STAFF',
}

export enum TAB_TYPES {
  DEFAULT = 'default',
}
